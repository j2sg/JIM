- JIM Requirements

      - Qt5 libraries with QtSql module.
      - Qt5 SQLite 3 database driver.
      - Qt5 headers with QtSql module header.
      - Makefile generation tool QMake.
        
        You can obtain Qt at http://www.qt.io/download/ or through
        your package manager on unix systems.

- Compilation

   - Generate a platform-specific makefile:

      qmake

   - Build the application:

      - On Unix:

         make

      - On Windows:

         mingw32-make

   
   Now we have the JIM binary ready to execute.

